const hashFunction = (string, max) => {
    // Start initializing: hash
    // and accumulating 'string.charCodeAt()'
    // and using '%' and 'max'

}

class HashTable {
    constructor(limit) {
        // Initialize: storage, storageLimit

    }

    print() {
        console.log(this.storage)
    }

    insert(key, value) {

    }

    lookup(key) {

    }

    remove(key) {

    }

    resize(newLimit) {

    }
}

module.exports = { hashFunction, HashTable }